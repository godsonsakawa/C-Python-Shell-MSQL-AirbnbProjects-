-- Lists all records of table second_table of the db hbtn_0c_0
-- Records should display both score and name in this order.
-- Sorts by score descending order. 
SELECT score,name FROM second_table ORDER BY score DESC;
